### Virtual Host Config
server {
    listen 80;
    listen [::]:80;
    location / {
        proxy_pass http://unix:/srv/socks/shiny.sock;
        proxy_redirect / $scheme://$http_host/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }
}